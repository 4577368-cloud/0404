# 商品目录 + 知识库：源码放在 public/data/（构建后访问 /data/*.json）。`tryFetchJson` 先请求站点 /data/，再 GitHub Raw。
# 未设置时使用默认：4577368-cloud/agent 仓库 main 分支根目录（与仓库根目录下文件名一致）。
# 若仅想走本地、不要 GitHub，请设为空： VITE_DATA_REMOTE_BASE=
# VITE_DATA_REMOTE_BASE=https://raw.githubusercontent.com/4577368-cloud/agent/main

# 部署在子路径时设置（GitHub Pages、子目录站点），否则入口 JS/CSS 会从域名根路径加载 → 404 白屏。
# 示例站点 https://user.github.io/my-repo/ 则： VITE_BASE_PATH=/my-repo/
# 根域名部署、Vercel 默认不要设或设为 /
# VITE_BASE_PATH=/
#
# Supabase：Project Settings → API
# VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key
#
# 匿名访客（与额度 / user_stats / 日志共用 auth.uid()，需在控制台开启）：
# Authentication → Providers → Anonymous → Enable。关闭时应用会回退到浏览器 localStorage 计数。
#
# ── 匿名 ID 与「剩余次数变回 30」说明（部署不会自动重置 Supabase 额度）──
# - 匿名登录成功后，Supabase 在 auth.users 里分配固定 UUID；public.user_stats 按 user_id 存 free_quota_used。
# - 会话 JWT 存在浏览器 localStorage（同站点 origin）。重新 Deploy / 换代码不会清空该存储。
# - 若你发现剩余次数「又回到 30」，常见原因不是 Vercel 本身，而是：
#   1) 换域名或子域（如 preview-xxx.vercel.app 与正式域名）→ 不同 origin → 新会话 → 新匿名用户 → 新一行 user_stats。
#   2) 清除站点数据 / 无痕模式 / 换浏览器或设备 → 同上。
#   3) VITE_SUPABASE_URL / ANON_KEY 指向了另一个 Supabase 项目 → 另一套用户表。
#   4) 匿名 Provider 未开启或会话失败 → 前端回退到本地 tangbuy_chat_used（30 上限），与数据库无关。
# 需要长期同一额度：固定线上域名、勿清站存；或 Google/Facebook 登录同一账号。
#
# Google OAuth（Supabase 控制台）：
# 1) Authentication → Providers → Google：填写 Client ID + Client Secret（来自 Google Cloud Console）
# 2) Google Cloud → 凭据 → OAuth 客户端 → 已获授权的重定向 URI 必须添加：
#    https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
# 3) Supabase → Authentication → URL Configuration：Site URL = 本地如 http://localhost:5179；
#    Redirect URLs 增加 http://localhost:5179 与线上域名
#
# Google 授权页出现 xxx.supabase.co / 文案不友好：前端无法改 Google 页面，需在「同一 Google Cloud 项目」里配置 OAuth 同意屏幕：
# Google Cloud Console → APIs & Services → OAuth consent screen
#   - App name：改为产品名（如 Tangbuy AI），用户会看到该名称作为主展示
#   - App logo、User support email、Developer contact 填全
#   - Authorized domains：添加你的官网域名（若有）
# 重定向仍指向 Supabase，部分说明里仍可能出现 supabase 域名；应用名称与徽标是主要观感来源。
#
# Facebook 登录（与 Google 相同走 Supabase OAuth，无需在页面加载 Facebook JS SDK）：
# 1) Meta for Developers → 创建应用 → 添加「Facebook 登录」产品
# 2) 应用设置 → 基本：记下应用编号；设置 → 高级 等处的 App Secret
# 3) Facebook 登录 → 设置 → 有效 OAuth 重定向 URI 添加：
#    https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
# 4) Supabase → Authentication → Providers → Facebook：启用并填入 Client ID（=应用编号）、Secret
# 5) 同上，Redirect URLs / Site URL 与 Google 一致（本地 + 线上）
#
# ── Vercel（Project → Settings → Environment Variables）──
# 构建时注入（Vite 会打进前端，勿放密钥）：
#   VITE_SUPABASE_URL
#   VITE_SUPABASE_ANON_KEY
#   VITE_DATA_REMOTE_BASE   （可选；不设则用默认 GitHub Raw agent/main）
# 仅服务端 /api/chat 使用（不要加 VITE_ 前缀）：
#   VLLM_BASE_URL
#   VLLM_API_KEY
#   VLLM_MODEL_ID
# 上述变量建议 Production / Preview 都配置；改完后重新 Deploy。
# Supabase 线上登录：Redirect URLs 增加 https://你的域名.vercel.app
